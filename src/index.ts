import { zfill, numberFormat, timeFormat, formatoFecha, formatoHora, solicitud, fechaToNumber } from './Formatos';
import { ToRGB, getMes } from './Converciones';
import { portapapeles } from './Acciones';
import { filtro, busqueda, indexOfCallback } from './Filtros';
export {
    zfill, numberFormat, timeFormat, formatoFecha, formatoHora, solicitud, fechaToNumber,
    ToRGB, getMes, filtro, busqueda, indexOfCallback, portapapeles
}
